# Example environment variables for production build
# Copy to `.env.production` and set values before building for production

# Base URL of the deployed Cloudflare Worker (or other proxy) without trailing slash.
# Example: https://manacheck-proxy.<subdomain>.workers.dev
VITE_WORKER_BASE=
